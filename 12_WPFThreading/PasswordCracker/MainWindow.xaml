<Window x:Class="HashGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hash Generator" Height="250" Width="400">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Label Content="Passwort:" Width="90"/>
            <TextBox x:Name="tbPassword" Width="250"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 10 0 0">
            <Label Content="Salt:" Width="90"/>
            <TextBox x:Name="tbSalt" Width="250"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Compute" Width="120" Height="30" 
                HorizontalAlignment="Left" Click="Compute_Click"/>

        <TextBox Grid.Row="3" x:Name="tbHash" Margin="0 10 0 0" 
                 IsReadOnly="True" FontSize="14"/>
    </Grid>
</Window>
